// Decimal to Binary Converter

#include <iostream>
using namespace std;

int main()
{
  // Var Declarations
  int numDec = 0;
  long long numBinary = 0;

  // User Prompt
  cout << "Enter a decimal number to be converted to binary: ";
  cin >> numDec;

  if(numDec = 0)
  {
    cout << numBinary << endl;
  }
  else
  {
    do {

      for (size_t numCount = 0; numCount < numDec; numCount++) {

        

      }
    } while(numDec != 0);
  }

  return 0;
}
